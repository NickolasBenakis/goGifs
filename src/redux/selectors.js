// import { createSelector } from 'reselect';

// const selectTerm = (state) => state.term;

// const selectGifs = (state) => state.gifs;

// export const selectCurrentTerm = createSelector([selectTerm], (term) => term);
// export const selectCurrentGifs = createSelector([selectGifs], (gifs) => gifs);
